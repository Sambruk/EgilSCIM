# Configuration file for the DNPActivity object.
# Each DNPActivity object represents one of the available
# test activities that should be set as parentActivity
# for the Activities sent to DNP.
# We will never send these objects anywhere, just load
# them into memory and link them to the groups so that we
# can get their id when we send the Activities.

DNPActivity-unique-identifier = id

DNPActivity-scim-json-template = <?
{}
?>
