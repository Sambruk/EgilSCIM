# Configuration file for the Organisation object

Organisation-scim-url-endpoint = Organisations

Organisation-unique-identifier = entryUUID

Organisation-scim-json-template = <?
{
"schemas": ["urn:scim:schemas:extension:sis:school:1.0:Organisation"],
"externalId": "${entryUUID}",
"displayName": "${o}"
}
?>
